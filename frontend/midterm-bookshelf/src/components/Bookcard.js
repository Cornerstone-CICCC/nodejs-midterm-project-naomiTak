import React from "react";

export const Bookcard = (props) => {
    const { books } = props;
    return(
        <div className="card">
            <h3></h3>
        </div>
    );
}

//export default Header;